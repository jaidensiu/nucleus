# Token Layout

Scalable token source structure for Nucleus.

- `primitive/` contains raw token values that are safe to consume directly today.
- `semantic/` is reserved for intent-based aliases.

The default build currently reads `primitive/` and `semantic/`.
